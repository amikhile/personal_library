= simple_form_for [@label] do |f|
  = display_actions f, labels_path
  %hr.soften
  = display_errors f

  .labels
    = f.input :name, :label => t('ui.content.name')
    = f.input :description, :label => t('ui.content.description')

  %hr.soften
  = display_actions f, labels_path